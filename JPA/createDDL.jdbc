CREATE TABLE DEPARTMENT (DEPT_ID INTEGER NOT NULL, NAME VARCHAR, PRIMARY KEY (DEPT_ID))
CREATE TABLE EMPLOYEE (EMP_EID INTEGER NOT NULL, DEG VARCHAR, ENAME VARCHAR, SALARY DOUBLE, DEPT_ID INTEGER, PRIMARY KEY (EMP_EID))
ALTER TABLE EMPLOYEE ADD CONSTRAINT FK_EMPLOYEE_DEPT_ID FOREIGN KEY (DEPT_ID) REFERENCES DEPARTMENT (DEPT_ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
